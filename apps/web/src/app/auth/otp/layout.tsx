import React, { ReactNode } from 'react';

const OtpPageLayout = ({ children }: { children: ReactNode }) => {
  return <div className='w-full h-screen'>{children}</div>;
};

export default OtpPageLayout;
