import { IMessageBubblePreview } from "@/Atoms/types/messageBubble.types"
import { FC } from "react"

export const MessageBubbleOtherFilesPreview: FC<IMessageBubblePreview> = ({ message, messageLines }) => {
  return (
    <div>
      other Files
    </div>
  )
}
